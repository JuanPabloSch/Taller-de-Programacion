# Proyecto Grupo 4 - Taller de Programación

CRUD con Django, DataTables y MySQL (ISDM Pagos).

## Instalación
```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
